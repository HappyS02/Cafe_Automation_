@{
    ViewData["Title"] = "Sipariş Alındı";
    Layout = "~/Views/Shared/_CustomerLayout.cshtml";
}

<div class="text-center mt-5 pt-5">
    <div class="mb-4">
        <i class="fas fa-check-circle text-success fa-5x animate__animated animate__bounceIn"></i>
    </div>
    <h2 class="fw-bold text-dark">Siparişiniz Alındı!</h2>
    <p class="text-muted">Mutfağa iletildi, en kısa sürede masanızda olacak.</p>

    <div class="mt-5">
        <a asp-controller="Home" asp-action="Menu" class="btn btn-warning rounded-pill px-5 py-3 fw-bold shadow">
            <i class="fas fa-arrow-left me-2"></i> Menüye Dön
        </a>
    </div>
</div>